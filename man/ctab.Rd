% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ctab.R
\name{ctab}
\alias{ctab}
\title{Cross table}
\usage{
ctab(
  d = parent.frame(),
  x,
  y,
  digits = 1,
  rowprct = FALSE,
  total = TRUE,
  n = TRUE,
  format = TRUE,
  drop = TRUE,
  file = NULL,
  ...
)
}
\arguments{
\item{d}{data.frame}

\item{x}{row variable}

\item{y}{column variable}

\item{digits}{number of digits to display}

\item{rowprct}{if \code{TRUE}, compute row percentages. Default \code{FALSE} computes column percentages.}

\item{total}{if \code{TRUE} (default), add a column with the sum of percentages and a row with global percentages.}

\item{n}{if \code{TRUE} (default), display number of observations per row.}

\item{format}{if \code{TRUE} (default), add a title and print a three-line table. If \code{FALSE}, return a tibble object,}

\item{drop}{if \code{TRUE}, lines or columns with a sum of zero, which would generate \code{NaN} percentages, are dropped.}

\item{file}{File name of MS Word (\code{.doc}). It doesn't work if \code{format = FALSE}}

\item{...}{parameters passed to other methods.}
}
\value{
The result is an object of class \code{table} and \code{proptab}.or a tibble if statistics argument is FALSE
}
\description{
Two-way frequency table with column (default) or row percentages, with Chi-2 test, p-value and Cramer's V. Support also R base language \code{table(d$x, d$y)} and native pipe \code{d |> table(x, y)}.
}
\examples{
\dontrun{
data(mtcars)
ctab(mtcars, cyl, vs)
ctab(mtcars, mtcars$cyl, mtcars$vs)
tab(mtcars$cyl, mtcars$vs)
mtcars |> ctab(cyl, vs)
ctab(mtcars, cyl, vs, rowprct = TRUE, total = FALSE, n = FALSE, statistics = FALSE)
ctab(mtcars, cyl, vs, total = FALSE, n = FALSE, statistics = FALSE)
}
}
